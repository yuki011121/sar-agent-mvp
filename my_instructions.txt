Run:
- docker compose up -d
- poetry run python -m agents.health.main
- poetry run python -m agents.health.test_health_agent

Redis:
docker exec -it sar-agent-mvp-redis-1 redis-cli
XLEN health.assessment.raw
XREVRANGE health.assessment.raw + - COUNT 1




Input Streams → Data Aggregation → LLM Analysis → Output Streams
     ↓              ↓                ↓              ↓
mission.new    HealthProfile    Gemini/MCP     health.assessment.raw
weather.forecast.raw    ↓        Tool-calling   logistics.requests.raw
field.observation.raw   ↓           ↓
                       Prompt    Structured JSON